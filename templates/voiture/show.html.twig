{% extends 'base.html.twig' %}

{% block title %}Voiture{% endblock %}

{% block body %}
    <h1>Voiture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ voiture.id }}</td>
            </tr>
            <tr>
                <th>Matricule</th>
                <td>{{ voiture.matricule }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ voiture.marque }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ voiture.model }}</td>
            </tr>
            <tr>
                <th>Numero Assurance</th>
                <td>{{ voiture.NumeroAssurance }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('voiture_index') }}" class="btn btn-outline-primary">back to list</a>

    <a href="{{ path('voiture_edit', {'id': voiture.id}) }}" class="btn btn-outline-info">editer</a>

    {{ include('voiture/_delete_form.html.twig') }}
{% endblock %}
