{% extends 'base.html.twig' %}

{% block title %}Editer une  Voiture{% endblock %}

{% block body %}
    <h1>Editer une Voiture</h1>

    {{ include('voiture/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('voiture_index') }}" class="btn btn-outline-primary">back to list</a>

    {{ include('voiture/_delete_form.html.twig') }}
{% endblock %}
